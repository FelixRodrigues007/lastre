# Plan execution — Final Round pack (2026-07-21)

Autonomous run of the P0/P1 improvement list.

## Done

| Item | Evidence |
|------|----------|
| Evidence jury mode | `lastCasperSettle`, `x402Mode`, `honesty`, `accessRights`, `mainnetRoadmap`, `sampleSettles`, `juryLinks` in `getEvidencePack()` |
| Honesty freeze UI | Marketplace note + demo steps; Agents hero |
| Access-rights copy | evidence + Agents + BUIDL paste |
| Mainnet honest line | evidence + BUIDL paste |
| Autonomy density | **7/7** ok on prod (`plan-exec-*`) |
| Real settle densify | `b1967b6379c67f64a1b4f28767450f18d9aaca137a841f8c2b107765c18f2106` |
| Jury smoke script | `scripts/jury-smoke.sh` |
| Video 60s kit | `docs/VIDEO_60S_SCRIPT.md` |
| Canonical settle hashes | casper-rpc allowlist updated |
| Monitor 5h | scheduler `019f863af4cf` |
| TG / ranking / Kraken | kits already updated earlier |

## Requires redeploy (Render API + Vercel app)

Code changes only go live after deploy of `app/server` and `app/src` (Marketplace/Agents).

```bash
# After deploy
bash scripts/jury-smoke.sh
curl -s https://app-api.lastre.io/api/evidence | jq '.lastCasperSettle,.honesty,.x402Mode'
```

## Manual only (Felix)

1. Dora **Edit BUIDL →** paste short description from top of `docs/BUIDL_PAGE_PASTE.md`
2. Optional: record 60s video from `docs/VIDEO_60S_SCRIPT.md`
3. Dry-run visual demo day with scorecard tabs open

## Not done (by design / P2)

- WCSPR + CSPR.cloud facilitator parity
- MCP `get_provenance`
- Redis autonomy persistence
- Collateral monitor (anti-pivot)
