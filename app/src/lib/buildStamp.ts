/**
 * Static build stamp for Cloudflare Pages cache-busting.
 *
 * This value is intentionally imported by `main.tsx` so Vite produces a new
 * hashed entry bundle when we need to invalidate a poisoned edge asset cache.
 * It is not user-facing product state.
 */
export const APP_BUILD_STAMP = "2026-07-15-app-asset-mime-hotfix";
